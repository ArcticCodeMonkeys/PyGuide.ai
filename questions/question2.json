{
  "title": "Test Dictionary",
  "description": "Given an array of integers, create a function which uses an O(n) algorithm to sort the numbers, and save the smallest in a variable named Minimum",
  "constraints": "Each input would have exactly one solution.",
  "examples": "Input: nums = [2, 7, 11, 15], Output: 2"
}
